# Configurações do Banco de Dados
DB_TYPE=sqlite
DB_PATH=/app/data/lovitask.db

# Configurações da Aplicação
APP_ENV=development
APP_PORT=5000
DEBUG_MODE=true

# Para rodar localmente no host, use um caminho do sistema de arquivos do host, por exemplo:
# DB_PATH=./src/LoviTask.Api/lovitask.db
